{
  "name": "transparent-llm-log",
  "version": "1.0.1",
  "description": "无感记录 LLM（Chat Completions）调用与回调结果",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["llm", "openai", "chat-completions", "logging", "audit"],
  "author": "cxy13h",
  "license": "ISC",
  "type": "module",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^25.3.5",
    "typescript": "^5.9.3"
  },
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  }
}
